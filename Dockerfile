# Canonical Dockerfile lives in docker/Dockerfile (repo-root copy kept for tooling).
# Build from repo root:  docker build -f docker/Dockerfile -t romance-stage03:latest .
# Or use transfer_bundle/ after: bash scripts/bundle/make_transfer_bundle.sh
