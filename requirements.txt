--extra-index-url https://pypi.nvidia.com

# Core ML & Optimization
bertopic==0.17.3
sentence-transformers==5.1.2
transformers==4.57.1
torch==2.9.0
bitsandbytes==0.48.2  # For 4-bit quantization of Mistral models
accelerate==1.12.0  # Required for device_map="auto" with quantization
numpy==1.26.4  # Downgraded due to tomotopy/octis requirements (requires numpy<2.0)
pandas==2.3.3
scikit-learn==1.7.2
numba==0.61.2
gensim==4.4.0
scipy==1.16.3

# Hyperparameter Optimization
# OCTIS is installed separately with --no-deps because its metadata pins
# scikit-learn==1.1.0, which conflicts with the BERTopic stack on Python 3.12.
scikit-optimize==0.10.2
nltk>=3.9

# Topic Modeling (OCTIS dependencies)
tomotopy==0.13.0
libsvm==3.23.0.4

# NLP & Text Processing
spacy==3.8.11
booknlp==1.0.8  # Book-length NLP (entity, coref, .book); uses project torch/CUDA
beautifulsoup4==4.14.2
EbookLib==0.19

# GPU Acceleration (RAPIDS)
# Ensure these versions are compatible with your CUDA driver
cuml-cu12==25.10.0
cudf-cu12==25.10.0
dask-cudf-cu12==25.10.0
cupy-cuda12x==13.6.0

# CPU Fallbacks for BERTopic
umap-learn==0.5.3
hdbscan==0.8.40

# CLI & Utilities
click==8.3.0
typer==0.20.0
rich==14.2.0
tqdm==4.67.1
tenacity==9.1.2
PyYAML>=6.0

# Monitoring & Visualization
gputil==1.4.0
flask==3.1.2
matplotlib==3.10.7
seaborn>=0.13.0
adjustText>=1.3.0
pytest-mpl>=0.17.0
Pillow>=10.0.0
colorspacious>=1.1.2

# Statistical Analysis
statsmodels>=0.14.0

# Stage10 final analysis (DuckDB aggregation, markdown report tables)
duckdb>=1.5.0
tabulate>=0.9.0

# Stage11 human-review PDF export
reportlab>=5.0.0

# API Clients
openai>=2.9.0
