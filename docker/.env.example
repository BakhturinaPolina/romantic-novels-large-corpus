# Copy to .env and set your keys. Never commit .env to git.

# Stage 08 (LLM labeling) / Stage 09 (taxonomy/Radway)
# https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Hugging Face Hub — private embedding dataset (Stage03 embeddings_hub)
# https://huggingface.co/settings/tokens
HF_TOKEN=
